% Included via --include-in-header for `my/markdown-export-pdf' (lang-markdown.el).
% Mirrors the `tikz' entry in org-latex-packages-alist in init-org.el, so a
% bare \begin{tikzpicture}...\end{tikzpicture} environment in the Markdown
% source renders natively when Pandoc compiles the PDF through LaTeX.
\usepackage{tikz}
